enum ErrorMsg {
    // Student
    //============================================================================
    STUDENTS_FETCHED = "An error occurred while fetching the students",
    STUDENT_REGISTERED = "An error occurred while registering a new student",
    STUDENT_UPDATED = "An error occurred while updating the student",
    // USER
    //============================================================================
    USER_NOT_FOUND = "An error occurred while updating the student",
    USER_INCORRECT_PASSWORD = "An error occurred while updating the student",
}

export default ErrorMsg;